{% extends 'base.html.twig' %}

{% block title %}ProductOrder{% endblock %}

{% block body %}
    <h1>ProductOrder</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_order.id }}</td>
            </tr>
            <tr>
                <th>Aantal</th>
                <td>{{ product_order.aantal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('product_order_index') }}">back to list</a>

    <a href="{{ path('product_order_edit', {'id': product_order.id}) }}">edit</a>

    {{ include('product_order/_delete_form.html.twig') }}
{% endblock %}
